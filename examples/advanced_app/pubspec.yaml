name: advanced_app
description: Advanced Flutter example for AI SDK Dart — demonstrates all providers, tools, image gen, multimodal, embeddings, TTS, STT.
publish_to: none
version: 1.0.0+1
resolution: workspace

environment:
  sdk: ^3.11.1
  flutter: ">=3.41.3"

dependencies:
  flutter:
    sdk: flutter
  ai_sdk_dart: ^2.0.0
  ai_sdk_provider: ^2.0.0
  ai_sdk_openai: ^2.0.0
  ai_sdk_anthropic: ^2.0.0
  ai_sdk_google: ^2.0.0
  ai_sdk_flutter_ui: ^2.0.0
  image_picker: ^1.0.7
  record: ^6.2.0
  audioplayers: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
