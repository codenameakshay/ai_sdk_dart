# v3 execution and qualification ledger

Baseline: `b702929811281ac6be4c5ea2c10004b412b74924`. Scope and acceptance criteria: [approved report](v3.0.0-report.md). A passing focused test does not satisfy release qualification. No package publication or merge is authorized by this ledger.

| Work | Scope | Status | Evidence / outstanding gates |
|---|---|---|---|
| W01 | Stream settlement and response-message correctness | Implemented; review pending | Parent core snapshot699 passed before current approval changes; targeted cleanup/replay tests pass; final suite pending |
| W02 | Embedding integrity and batching | In progress | Core integrity, provider cardinality, indexed OpenAI parsing, queue tests; cancellation and provider-limit qualification pending |
| W03 | v3 contract and migration decisions | Decision recorded; verification pending | `docs/adr/0005-v3-public-contract.md` freezes results/events/context/files/lifecycle/persistence and migration alias policy; compiled generation/custom-provider examples and field matrix exist; canonical streams/files and final migration audit pending |
| W04 | Uniform request lifecycle and schema validation | In progress | Core nontext abort/deadline propagation, structured-stream terminal fixes, optional schema adapter implemented; text startup/step/meaningful-first-chunk deadlines corrected and parent-tested; provider observer lifetime, final integration and migration gates pending |
| W05 | V7 result/event/tool policy alignment | In progress | Aggregate collections/usage and finalStep landed; parent corrected finish-event usage and per-step messages/metadata with tests. Canonical instructions/callbacks landed; exact approvals and distinct typed contexts are under review; legacy fixture migration and canonical streams pending |
| W06 | OpenAI Responses | In progress | Responses adapter and fixtures in review; duplicate-call fix, complete hosted-tool lifecycle and default migration qualification pending |
| W07 | Claude/Gemini current reasoning and native output | In progress | Real Gemini function-call ID/thought-signature and Anthropic signed/redacted reasoning replay fixtures landed; parent flagship190 passed before newer Files edits; wider capability/live qualification pending |
| W08 | Other provider correctness and capability descriptors | Implemented slices; qualification incomplete | Shared cancellation/lazy auth integrated; parent seven-provider suite239passed and shared Chat65passed. Broader per-operation capability tables and direct lifetime matrix remain |
| W09 | Typed conversation/persistence codec | Implemented; integration qualification pending | Deep JSON immutability/bounds, codec/IDs/extensions corrected after review; parent ran 8 tests and JSON persistence example, clean analysis. Remote/UI/local-agent integration remains |
| W10 | Remote/Vercel transport companion | In progress | Parent reference-server run passes 15 tests; auth cancellation, peer socket closure and detachable caller observation added; local/UI integration remains |
| W11 | Modern MCP and replay-safe requests | Implemented; qualification incomplete | Modern result/MRTR validation, stateless headers, subscriptions/listen lifecycle, and operation-aware replay tests pass; legacy TypeScript 1.30.0 reference passes; broader external conformance remains |
| W12 | MCP auth/progress and selected extensions | In progress | Issuer/protected-resource discovery, duplicate challenge rejection, concurrent refresh coalescing, typed monotonic progress, and host credential boundaries are covered; Tasks/cache hints/partial output remain explicitly unsupported and broader extension audit remains |
| W13 | Structured-stream and Flutter performance | In progress | Parent 30-run rotating parser benchmarks plus30fresh-process retention samples/mode recorded; element-only allocation and late-subscriber tests pass. Flutter frame, cancellation/event-delivery profiling and calibrated thresholds remain |
| W14 | Framework recipes and accessibility | In progress | Dev-only Riverpod/Bloc recipes and disabled-stop fix exist; parent rejected reactive/lifecycle gaps; real framework tests and platform evidence pending |
| W15 | Observability and body minimization | In progress | Typed default-off body policy and OTLP exporter implemented; parent exporter19passed after retry identity/resource retention corrections. Full operation/privacy matrix and external collector qualification remain |
| W16 | Model catalog, docs, compatibility automation | In progress | Parent tooling tests 7 passed; analysis clean; catalog check validates 16 records/9 providers. Weekly offline freshness check added; broader compatibility coverage and release docs pending |
| W17 | Files and first batch adapter | In progress | Parent Files/Batch40passed after actual-client observer cleanup tests and batch wire corrections. Remaining timeout/registration observer edge proofs, current live qualification and packaging remain |
| W18 | Realtime voice preview | Implementation in review | Optional package registered in workspace/test matrix; first draft rejected for missing transport auth and lifecycle gaps. Authenticated loopback handshake added; lifetime, bounds, identities, typed events and device proof remain incomplete |
| W19 | Enterprise/new provider expansion | Admission review recorded; demand/owner pending | `docs/proposals/provider-admission.md` records candidate-specific prerequisites and proof; no named adopter/owner or new verified-support claim |
| W20 | Durable workflow/harness/A2A/MCP Apps | Separate proposals written; admission pending | `docs/proposals/` defines boundaries, adopter/ownership gates, failure semantics and prototype proof for all four; no named adopter exists, no support claim, and these remain outside core GA per approved plan |

## Execution constraints

Initial Luna requests failed before execution with `model_not_found`. Fresh `gpt-5.6-luna` workers now execute successfully and own the active implementation tasks. `poteto-mode` was not found in the installed skill directories.

## Release gates

- [ ] Full tests, analysis, formatting, benchmark, coverage, package dry-runs
- [ ] Requirement-by-requirement core/provider/protocol fixtures
- [ ] Pinned upstream differential checks and remote reference server
- [ ] Live-provider canaries, with dated evidence and controlled spend
- [ ] Platform, UI accessibility, and realtime device qualification
- [ ] Before/after measurements and documented tradeoffs
- [ ] Independent standards/spec and no-comments review
- [ ] Commit, push, PR with evidence; do not merge or publish

Detailed current proof and tradeoffs: [reliability evidence](v3-research/reliability-implementation-evidence.md).

Luna service recovery: a fresh explicitly selected `gpt-5.6-luna` worker launched and completed a remote-auth cancellation review on 2026-09-23. Earlier startup failures do not describe current availability. It now owns the confirmed remote cancellation corrections.

Independent review findings: [checkpoint log](v3-review-checkpoints.md).

Environment qualification: `fvm flutter devices` currently reports Linux desktop only. OPENAI_API_KEY, ANTHROPIC_API_KEY and GOOGLE_API_KEY are not configured (presence only checked, no values printed). Live canaries and iOS/Android device qualification remain missing evidence, not replaced by mocks. `git fetch origin` found no commits beyond the baseline on main; draft PR #4 remains the only open PR.

Additional parent verification: provider contract suite 30 tests passed; conversation suite 8 passed; aggregate-result review regressions 2 passed. Conversation persistence and custom embedding provider examples ran successfully. These focused results are not a current full-matrix pass. An attempted full run encountered a conversation file mid-edit and was stopped; rerun when owners finish.

Historical checkpoint, superseded by later runs below: the pinned JavaScript reference server (`ai` 7.0.111) was started locally after `npm ci`; `AI_SDK_REMOTE_REFERENCE_URL=http://127.0.0.1:8081/chat fvm dart test packages/ai_sdk_remote/test` passed all 9 tests, including request validation and approval history. `fvm dart test packages/ai_sdk_mcp/test` passed all 138 tests, including protected-resource and authorization-server discovery. The core worker reports 689 core tests passing after strict final-JSON expectation corrections; an independent final run remains pending. Review found that text deadlines do not cancel provider work and remote auth-header acquisition is not cancellable; both are assigned for correction before release qualification.

Parent integration observation (2026-09-23): `make analyze` failed in Flutter UI test helpers because new agent named parameters and required result `finalStep` were not migrated. Core also reported three lint infos. Owners have the diagnostics; this is not a green final matrix. Open draft PR #4 remains unchanged and is the sole open PR.

Additional verified packaging/example checkpoint: conversation and remote package dry-runs both exited0 with zero warnings. The new `packages/ai_sdk_remote/example/example.dart` ran against the pinned JS reference backend and printed its expected greeting; focused analysis and format check passed. The approval replay prevalidation regression passed after fingerprint recomputation was corrected. Files focused suite20 passed independently. None of these substitute for final full-matrix gates.

Web compilation checkpoint: `fvm dart compile js packages/ai_sdk_remote/example/example.dart -o /tmp/v3-remote-example.js` exited0. This verifies the pure Dart remote example compiles for web; browser runtime/auth/cancellation qualification remains separate. `git diff --check` passed at this checkpoint.

Current ownership: fresh Luna `approval_contract_completion` replaces the stopped `responses_adapter` for remaining W05 tests/context/replay corrections; `provider_readiness` owns Flutter lifecycle/helper migration and subsequent local approval resume; `luna_availability_check` owns OpenAI Files edge cases and experimental batch. W15/canonical stream, W03 media variants, W08 provider lifecycle, W13 final benchmarks, W18 implementation and final release gates remain pending; preserve their scope.

Latest ownership supersedes the preceding historical entry: `core_release_contract_finish` owns canonical stream alias and W15 metric/exporter completion; `conversation_release_proof` owns Flutter/conversation approval replay, framework lifecycle and remote media integration; `realtime_lifecycle_finish` owns W18 lifetime/boundedness completion. All three are fresh Luna workers with high reasoning effort. Earlier owners are completed and should not be assumed active from their names in historical evidence.

Parent lifecycle checkpoint: `fvm dart test packages/ai_sdk_openai/test/openai_batch_test.dart packages/ai_sdk_openai/test/openai_files_test.dart packages/ai_sdk_openai/test/openai_batch_wire_regression_test.dart packages/ai_sdk_openai/test/responses_cancellation_test.dart` passed **41** tests (`/tmp/v3-openai-lifecycle-parent-current.log`). This includes the formerly failing pre-cancelled Responses auth regressions. Full OpenAI and Flutter suites still need reruns after W15 signature migrations settle. Parent duplicate-approval regression initially failed to load because Flutter fake agents lacked the new `bodyInclusion` argument; it is not yet runtime proof.

Subsequent parent checkpoint: full core **714 passed** (`/tmp/v3-core-w15-parent-tests.log`), full OpenAI **111 passed** (`/tmp/v3-openai-parent-after-w15.log`). The two parent Flutter race regressions also passed (`/tmp/v3-ui-parent-races-current.log`): duplicate approval during a gated tool execution runs the tool once, and concurrent Bloc replacement leaves the final subscription active. The duplicate test first failed to compile, then ran after the worker's guard was present; do not claim a reproduced runtime failure. Core analysis compiles but reports four lint infos. W15 assertions still need direct provider-stream, object, callback and raw-inclusion proof; these counts do not close those requirements.

Browser checkpoint: the advanced Flutter example built for web successfully and its offline approval fixture passed real Chromium keyboard-approval and pointer-denial checks at desktop/narrow viewport sizes, with zero captured page errors. Screenshots and exact limitations are in `v3-research/evidence/browser/README.md`. This is fixture UI proof, not persisted-tool execution, live-provider evidence, mobile-device evidence, or final-build qualification. The source is still changing.

The remote pre-listen cancellation correction has independent proof: the focused `remote_lifecycle_test.dart --name 'cancellation before listen'` test passed (`/tmp/v3-remote-prelisten-parent.log`). New parent remote media and shared Chat auth regressions are deliberately red and assigned/queued; do not report the current whole workspace as green. The next provider-lifecycle sweep must include `ai_sdk_openai_compatible/test/auth_cancellation_review_test.dart` and the audit in `v3-research/provider-lifecycle-followup.md`.

Parent checkpoint after Luna availability recheck (2026-09-23): full core **720 passed** (`/tmp/v3-core-parent-current.log`); conversation codec plus parent remote media regressions **14 passed** (`/tmp/v3-media-conversation-parent-current.log`); Flutter approval/restore plus duplicate-executor and Bloc replacement suites **12 passed** (`/tmp/v3-ui-approval-parent-current.log`). Remote byte data URLs and rejection of opaque provider references now have independent green proof. The multiapproval test asserts both tool-result IDs in continuation history; a stronger fresh-model/payload assertion is requested. These focused results do not qualify the whole workspace.

Registered optional `ai_sdk_telemetry` in workspace, test/analyze, coverage and publication dry-run lists; added realtime to coverage measurement. `fvm flutter pub get` passed. Exporter review found concurrent buffer eviction can invalidate post-send removal, staged timeouts omit response draining, disposal can inherit a longer existing flush, and cumulative sums incorrectly represent independent observations. The telemetry owner is correcting these before release acceptance. The UI owner is implementing the actual prebuilt-widget `ConversationBackend` path; the realtime owner continues bounded queue/lifetime corrections. Provider auth and Responses structured-error red regressions remain queued.

Realtime parent checkpoint: `fvm dart test packages/ai_sdk_realtime/test/event_queue_review_test.dart packages/ai_sdk_realtime/test/startup_lifetime_review_test.dart` passed **4** (`/tmp/v3-realtime-parent-latest.log`). The two previously red paused-queue regressions now pass unchanged: queued text precedes EOF/done, and overflow is a typed observable terminal failure. Session identity/acknowledgement ordering, audio budget recovery, fake-clock deadline proof and broader typed protocol/device qualification remain incomplete and assigned.

Current pinned-reference integration checkpoint: the local Node server was confirmed responding at `127.0.0.1:8081`; `AI_SDK_REMOTE_REFERENCE_URL=http://127.0.0.1:8081/chat fvm dart test packages/ai_sdk_remote/test` passed **23 tests**, no reference skips (`/tmp/v3-remote-reference-parent-current.log`). The server pins `ai` 7.0.111 and uses its `validateUIMessages` and UI stream creation functions. This proves transport consumption/request validation and scripted tool/approval frames, not actual hosted-tool execution or the forthcoming widget bridge.

Parent integration checkpoint: Responses error conversion now preserves provider message/code/type and actual wire status for failed objects and both streaming failure shapes. Its **3 reproduced failures are green**; full OpenAI **114 passed** (`/tmp/v3-openai-parent-errors-fixed.log`). W13 snapshot builder now initializes only on demand and imports the complete accumulated prefix. The formerly red structural-allocation assertion passes; full core **720 passed** (`/tmp/v3-core-parent-lazy-fixed.log`). Core/OpenAI analysis reports **no issues** after minor fixture/example migrations (`/tmp/v3-core-openai-analyze-parent.log`). Full W06 hosted-tool/ID work and W13 benchmark/frame evidence remain open; these corrections do not narrow those requirements.

Optional-package parent checkpoint: realtime full suite **17 passed** in the parent run (`/tmp/v3-realtime-parent-full-current.log`); both realtime and telemetry analysis report **no issues** (`/tmp/v3-optional-analyze-parent.log`). A worker's temporary realtime lint-rule overrides were rejected, removed, and the actual source corrected. Do not report suppressed lints as qualification.

Telemetry review reproduced three further failures beyond the worker's initial tranche: valid observations formed an oversized unsendable batch; connection and response stages each received the whole deadline; disposing during a stalled connection could wait for the older flush deadline. Parent corrected byte-aware batch splitting and whole-operation timeout/disposal boundaries. Full exporter **14 passed** (`/tmp/v3-telemetry-parent-final-tranche.log`), including deterministic fake-clock tests. Logs before correction: `/tmp/v3-telemetry-batch-parent-before.log`, `/tmp/v3-telemetry-deadline-parent-before.log`, `/tmp/v3-telemetry-dispose-parent-before.log`. Provider lifecycle sweep is now owned by `core_release_contract_finish`; hosted Responses/strict identities by `realtime_lifecycle_finish`; actual backend widget path remains with `conversation_release_proof`. Root owns further telemetry review/integration.

W13 parent benchmark checkpoint: harness now rotates case order, runs three warmups and **30 samples per case**, emits raw samples and nearest-rank p50/p95/p99, and enforces operation budgets for every run. `make benchmark` exited0. Results/source hashes/environment are saved in `v3-research/evidence/benchmarks/`; 1 MiB structural median112.036ms versus copy155.400ms, but small-case maxima regressed. This is a shared-VM, same-source algorithm comparison, not a calibrated whole-SDK speed claim. Heap-byte, cancellation/serialization and Flutter frame measurements remain open.

Stable-package checkpoint: `fvm dart test packages/ai_sdk_mcp/test packages/ai_sdk_json_schema/test tool/provider_capability_catalog_test.dart` passed **168 tests** (`/tmp/v3-stable-packages-parent.log`). `make catalog-check` validates16records/9providers. Subsequent W16 correction adds scheduled `--fail-on-stale`: the parent regression reproduced a stale full catalog incorrectly exiting0, then passed after the strict mode was wired into the weekly workflow. Catalog tooling now **8 passed**, analysis clean, and the current catalog passes strict mode. Local age warnings remain advisory; this is evidence-age enforcement, not automatic discovery or live-model qualification.

MCP coverage measurement: full139tests passed under coverage collection, with1177/1250 measured lines (**94.16%**). See `v3-research/mcp-coverage-checkpoint.md` for commands and behavioral gaps. This does not pass or fail the unmeasured aggregate99% gate; it identifies remaining meaningful protocol/lifecycle test work.

W11 parent external-reference checkpoint: added pinned TypeScript MCP SDK1.30.0 fixture and `make test-mcp-reference`; the real stdio handshake/discovery/tool success/error/recovery test passes. Five new modern HTTP loopback tests pass; full local MCP144passed and measured coverage improves to95.68% (1196/1250). The reference test is explicitly opt-in and was actually run; ordinary tests skip it without the environment flag. Modern external conformance and remaining W12 semantic checks are not closed by this legacy reference.

Parent provider follow-up: corrected two reproduced cancellation-registration races; provider34passed, shared Chat65passed with per-subscription direct-adapter retention proof. Hosted Responses review is not accepted yet: a new computer-call identity regression is red against the current mapping (client-executed computer action incorrectly classified as hosted). Independent Luna review `responses_independent_review` is active alongside W07 and UI owners.

Current browser checkpoint: rebuilt `examples/flutter_chat` and ran actual local-backend approval/denial plus pinned-JS remote text through the prebuilt scaffold. All three behavioral flows pass with zero page errors and saved build/source hashes. Browser preflight reproduced a missing allowed protocol header in the JS fixture; corrected CORS and reran successfully. Visual inspection then found duplicate assistant rows caused by the conversation adapter exposing history and optimistic text together; assigned for correction. See browser evidence README. No device/live/restore claim follows from this checkpoint.

Independent Files/Batch review confirmed non-detachable caller cancellation observers in `OpenAIRequestScope`; Luna owner `luna_availability_review` is correcting this with actual-client retention tests. Parent telemetry regression `observation_identity_review_test.dart` is red: one admitted observation invokes redaction three times across first export and retry, changing retry data; timestamp retention is also under review. The current tree is not fully green.

Subsequent parent verification: MCP147passed/1opt-inreference skipped; Files/Batch40passed; seven provider packages239passed; OpenAI134passed before the added hosted-replay regression. The latter is red: a hosted web search call/result pair replays the same raw wire item twice. W06 remains open and assigned. Full `make analyze` encountered the telemetry owner mid-edit, so that run is not a valid final gate.

Telemetry retry identity correction now passes; parent additionally reproduced retained unredacted resource attributes, removed that second raw copy, and replaced a tautological timestamp assertion with recording-time bounds. Full exporter19passed (`/tmp/v3-telemetry-parent-resource-fixed.log`); exporter and new retention benchmark analysis clean. Snapshot retention benchmark30samples/mode completed: median post-GC heap delta64,277,072bytes copy versus996,296bytes structural for4,000retained integer prefixes. Raw source hashes/results and limitations are saved in benchmark evidence; this is not whole-SDK or device memory qualification.

Further parent lifetime checks: Files observer tests now7passed, including timeout during headers and cancellation during registration with zero header calls and zero retained observers (`/tmp/v3-files-registration-parent.log`). Ollama still uses future-only cancellation in generate/stream/embed; all3new direct-client observer regressions fail (`/tmp/v3-ollama-observers-before.log`). Assigned to the core/lifecycle worker after bounded tool concurrency; the earlier provider239pass does not cover or close this newly reproduced gap.

Next stable checkpoint: parent full core plus Ollama **763 passed** (`/tmp/v3-core-ollama-parent-concurrency.log`) after bounded tool concurrency and detachable core/Ollama cleanup. Browser rebuild exited 0 in 84.4 seconds. Duplicate assistant rendering is visibly corrected, but Chromium's accessibility tree exposes the remaining selectable answer as an empty disabled textbox. Real remote approval/denial continuation returns HTTP 200 and renders output, while browser console reports a Flutter text-editing null-check exception. These are open W14 failures, not accepted browser qualification; the test now checks console errors as well as page errors. Before/intermediate screenshots are preserved.

W03 migration is active across three owners: provider/adapter contracts (`responses_independent_review`), core and Ollama (`luna_availability_review`), and conversation/remote/UI (`conversation_release_proof`). Provisional inheritance variants with empty text/URL compatibility projections were rejected. Direct canonical variants and exhaustive lossless boundary mappings are required. W06 hosted replay deduplication now has worker proof, but rich native tool output/computer validation remains under the canonical migration. Do not describe a JSON string containing an image URL as native multimodal tool output.

Parent W18 follow-up: reproduced `cancelResponse(responseId: ...)` omitting its ID from the outgoing command, then corrected direct cancellation and interruption. Removed the old beta header to match the pinned official OpenAI connector's current handshake. Full realtime **18 passed**, analysis clean (`/tmp/v3-realtime-parent-ga-fixed.log`). Source references and remaining typed/boundedness/device/packaging gaps are in `v3-research/realtime-qualification-gaps.md`; this does not close W18.

Parent canonical-adapter checkpoint: OpenAI/provider/Anthropic/Google/Cohere/shared-Chat suites **402 passed** (`/tmp/v3-parent-canonical-adapters.log`). Independent review then reproduced four missing native Responses media fields: file/image bytes and URLs were emitted as `base64`/`url` rather than `file_data`/`file_url`/`image_url`. New `responses_media_wire_review_test.dart` fails all four before correction; saved evidence is `v3-research/evidence/review/responses-media-wire-before.log`. Final-step reasoning was also found incorrectly aggregated against ADR0005 and assigned for correction. These findings prevent accepting the migration solely from the previous green suites.

Release-gate checkpoint: `make analyze` stopped on the active realtime usage-type migration (`realtime_test.dart:266`); rerun after stabilization. `make dry-run` stopped at the provider package's dirty-git warning (exit65), before checking the remaining packages; it is not a completed packaging gate and must run after coherent commits. GitHub inspection still shows only obsolete draft PR#4, with no v3 implementation PR.

PR15 is open in draft at https://github.com/codenameakshay/ai_sdk_dart/pull/15 and receives follow-up commits. Parent OpenAI145passed after shared prompt/tool media serialization, then33focused passed after final cleanup; analysis clean. Parent telemetry/schema48passed with stronger bounds/ownership tests; isolated worker coverage reports schema100% and telemetry92.79%. Full test run passed all Dart packages but reached active Flutter edits; later stable UI/examples run exposed a read-only property fixture mismatch and unhandled advanced-example `finalStep` fixture errors. Parent corrected the latter to a real successful step; all10advanced-example tests then passed.

Full coverage checkpoint failed before final aggregate because Flutter tests failed. Measured Dart subtotal9501/10422=91.16%, with per-package evidence in `v3-research/evidence/review/coverage-pr15-checkpoint.json`. This is incomplete, changing-source evidence, not an accepted final measurement. Threshold remains99%; behavioral remediation is active. `make benchmark` passed again, but ran alongside coverage so these timing samples are not used to supersede the isolated comparison evidence. Browser rebuild138.7seconds succeeded; the first approval flow still reports the same web text-input null check. The enabled/readOnly hypothesis did not fix it, and investigation continues.

Parent keyed-editor checkpoint: pinned Flutter3.44.3 web release build passed in55.6seconds; all five conversation browser flows pass with disabled pending input, restored editability, one assistant row, accessible output, and zero console/page errors. Parent widget suite144passed and widget analysis clean. Browser evidence includes build/source hashes and screenshots. W09 persisted replay metadata/signature gaps and native/live qualification remain open. PR15 body was refreshed with current verified checkpoints and explicit release blockers.

Parent Responses continuation checkpoint: OpenAI and shared-compatible combined suite227passed with pinned Dart3.12.2; analysis clean. Genuine generateText/streamText citation-plus-client-tool two-step regressions preserve document sources in public results while excluding citation items from provider input. Computer screenshot output now places acknowledged_safety_checks beside output, matching pinned upstream converter lines1462–1476; malformed acknowledgements fail locally. Isolated worker coverage: OpenAI92.43%, shared-compatible99.35%; neither establishes the final aggregate99%gate.

Native harness checkpoint: added an offline iOS Simulator integration target for local/remote text and approval/denial plus error dismissal. The macOS PR workflow runs twice, retains screenshots and drive logs, and records actual Flutter/Xcode/simulator metadata. Pinned dependency resolution and parent focused analysis pass. Native execution is not yet proven; CI is the next validation step. Retry remains an explicit app gap and is not counted as covered by dismissal.

Azure Responses parent audit: two new loopback regressions reproduced `/openai/deployments/<id>/responses` with an obsolete dated API query. Current Microsoft REST documentation uses `/openai/v1/responses`, model deployment in the body, and no dated query. Corrected Responses mapping while preserving Chat/embedding apiVersion behavior. Full Azure23passed, analysis clean. Before/after logs and official-source digest are in review evidence. Expanded provider surface table distinguishes factory/fixture evidence from unperformed live qualification.

Pushed checkpoint `dd14f09`: core replay metadata and provider-executed approval prevalidation, rich conversation codec, remote history/metadata, Azure Responses endpoint, v3 manifest alignment, migration examples and compatibility/canary workflows are included. Parent codec14passed; focused replay/provider11passed; remote metadata44passed, then dynamic/defensive expansion50passed; scoped analysis clean. Remote isolated coverage is593/602lines(98.50%), not the aggregate99%gate. Flutter retry/localization remains local and under review.

Native run35878618109 ended cancelled after its30minute job limit. Xcode15.4 built successfully in74seconds, but the log contains no completed test and no screenshot. Added verbose driver logs, a15minute step bound within the30minute job, and always-captured simulator screenshot/process logs. Run35882901359 is the next diagnostic attempt; it is not yet proof. Minimum-toolchain run35882901385 exposed flutter_riverpod^3.4.3 requiring Dart3.12 despite the declared3.11.1minimum. Changed the dev dependency lower bound to^3.3.2 (official pub metadata supports Dart^3.7), preserving newer3.xselection on newer SDKs; minimum-lane rerun is still required. Regular CI stopped on formatting in the uncommitted Flutter tranche and one remote review fixture; the remote fixture is now formatted.

2026-09-23 checkpoint `b50a8ab`: draft PR15 now includes the Dart remote backend fixture (parent 4 tests passed and scoped analysis clean), cross-origin browser framing/header proof, and refreshed five-flow browser evidence with zero page/console errors. Minimum-toolchain dependency failures were traced first to Riverpod and then test/test_api constraints; both lower bounds are corrected. Compatibility run35887584291 has passed minimum and latest lanes, with pinned still running at observation. Native run35886105499 connected and resumed the app successfully, then failed integration assertions; this replaces the previous discovery blocker but does not establish native qualification.

Independent review of `b702929...11155a3` remains split by axis. Standards found contradictory pending approval plus completed result replay, interrupted approval restoration, and escaping cancellation cleanup errors; the first two are in remediation. The cleanup finding was reproduced in two provider regressions before correction, then parent focused provider/core tests17passed with clean scoped analysis (`bccc98e`; logs in review evidence). Spec review found missing local tool results in chronological content and streamText request history filtering; both require contract remediation and regression evidence. Parser review separately confirmed incomplete object previews were withheld until the root closed; implementation and streaming parity review continue. None of these focused results closes the final release gates.

2026-09-23 resumed checkout checkpoint: the Ubuntu handoff's four local commits and uncommitted worker files were unavailable in this Mac worktree, which started clean at pushed `e0931d7`. The parser now emits immutable raw object previews before root closure, preserves growing strings and trailing spaces, and decodes the schema only on strict final output. A red-before growing-string regression and the full core suite (**763 passed**) qualify this local slice. The first benchmark caught excessive parse attempts; geometric checkpoints now pass its operation budget. The same-source 1 MiB comparison measures incremental p50 **196.141 ms** versus final-only **93.734 ms** on a shared Mac, a documented CPU cost for earlier previews; see the raw benchmark artifact. `make test`, `make analyze`, and `make format-check` pass on the changed tree. `make coverage-check` fails at **12,695/13,695 = 92.70%**, below the unchanged 99% gate (`/tmp/ai-sdk-v3-coverage.log`). The original six iOS flows passed twice locally after waiting for animated composer transitions; a focused native text retry passed with stable IDs. A new real-backend pending-approval test passed its write phase locally, but separate `flutter test` installs reset the app container. The updated build-once CI workflow must prove a distinct restore phase and eight screenshots per run. Local Xcode 27 required an uncommitted iOS 15 deployment override for simulator execution; the tracked iOS 13 target was preserved for the established macOS-14 CI runner. These checks do not establish release qualification.

Exact pushed head `d4b65e6`: [native run 35897437862](https://github.com/codenameakshay/ai_sdk_dart/actions/runs/35897437862) passed both app launches and retained nine screenshots per run, including distinct approval-write and restored-approval states and safe text retry. Parent downloaded and visually checked the three representative images now in `v3-research/evidence/ios/`. [Compatibility run 35897437827](https://github.com/codenameakshay/ai_sdk_dart/actions/runs/35897437827) passed all configured lanes. [CI run 35897438007](https://github.com/codenameakshay/ai_sdk_dart/actions/runs/35897438007) failed only the unchanged coverage gate at **12,691/13,692 = 92.69%** on Linux. Local follow-up codec malformed-wire tests and a red-before split-Unicode preview guard are not pushed yet; the newer parser source was rebenchmarked at 1 MiB p50 **126.211 ms** incremental versus **63.066 ms** final-only (30 samples, same shared Mac). The earlier d4 benchmark figures above remain a historical checkpoint, not the current artifact. No final-head review, browser flow rerun, frame/raster trace, credentialed canaries or 99% proof exists.
